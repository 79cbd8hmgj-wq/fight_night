name: Task 4 import order

on:
  push:
    branches:
      - task-4-iso-workspace

permissions:
  contents: write

jobs:
  cleanup:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: task-4-iso-workspace
      - name: Apply Ruff import ordering
        run: python -m pip install ruff && python -m ruff check --fix src/fnr3_re/iso.py
      - name: Commit cleanup
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add src/fnr3_re/iso.py
          git diff --cached --check
          git commit -m "style: order ISO workspace imports"
          git push origin HEAD:task-4-iso-workspace
